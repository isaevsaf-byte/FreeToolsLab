# deepcure

Set to `planned` in `tools/registry.json` on 2026-09-04: it was marked `live` but no code exists in this repo and
https://freetoolslab.org/tools/deepcure/ returns 404 (the old landing linked it too, also 404).
To ship it: `npm run new deepcure` (the registry entry is reused), build it per `tools/_template/README.md`,
then set status `live` + `added` date.
